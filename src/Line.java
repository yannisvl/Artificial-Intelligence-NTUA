import java.util.ArrayList;

public class Line {
    int id, lanes, maxspeed;
    String highway, name, oneway, lit, railway, boundary, access, natural, barrier, incline, waterway, busway, toll,
    tunnel,bridge;
    String[] traffic = new String[12];
}
